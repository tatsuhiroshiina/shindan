<title>
  JETRO 支援メニュー診断
</title>
<h1>JETRO 支援メニュー診断</h1>
<div class="page-enter">
<div class="form-container">
  <div class="header">
    <ul>
    <% @form.errors.full_messages.each do |message| %>
     <li class='error-message'><%= message %></li>
    <% end %>
    </ul>
  </div>
  <div class="form-box">
  <%= form_for(@form) do |f| %>
    <b>法人名</b>
      <div class= "each-answer field-container">
      <%= f.text_field :field, class: "name-field"%>

      </div>
      <br>
    <b>プロダクト分類</b>
      <div class= "each-answer">
      <span>ハードウェア</span>
      <label><%= f.radio_button :product_type, :ハードウェア, checked: true, class: "radio" %></label>
      <span>ソフトウェア</span>
      <label><%= f.radio_button :product_type, :ソフトウェア, checked: false, class: "radio" %></label>
      <br>
      </div>
    <b>顧客</b>
      <div class= "each-answer">
      <span>法人向け</span>
      <label><%= f.radio_button :company_type, :法人向け, checked: true, class: "radio" %></label>
      <span>消費者向け</span>
      <label><%= f.radio_button :company_type, :消費者向け, checked: false, class: "radio" %></label>
      <br>
    </div>
    <b>シリーズ</b>
      <div class= "each-answer">
        <span>シード</span>
        <label><%= f.radio_button :series, :シード, checked: true, class: "radio" %></label>
        <span>シリーズA</span>
        <label><%= f.radio_button :series, :シリーズA, checked: false, class: "radio" %></label>
        <span>シリーズB</span>
        <label><%= f.radio_button :series, :シリーズB, checked: false, class: "radio" %></label>
        <br>
        <span>シリーズC〜</span>
        <label><%= f.radio_button :series, :シリーズC〜, checked: false, class: "radio" %></label>
        <span>スタートアップではない</span>
        <label><%= f.radio_button :series, :スタートアップではない, checked: false, class: "radio"%></label>
        <br>
      </div>
    <b>事業分野(最も当てはまるものを1つ)</b>

      <div class= "each-answer">
        <span>環境・エネルギー</span>
        <label><%= f.radio_button :industry, :環境・エネルギー, checked: true, class: "radio" %></label>
        <span>バイオ・医療</span>
        <label><%= f.radio_button :industry, :バイオ・医療, checked: false, class: "radio" %></label>
        <br>
        <span>大学発ディープテック</span>
        <label><%= f.radio_button :industry, :大学発ディープテック, checked: false, class: "radio" %></label>
        <span>その他</span>
        <label><%= f.radio_button :industry, :その他, checked: false, class: "radio" %></label>
        <br>
      </div>
    <b>関心のある地域（複数選択可）</b>

      <div class="each-answer">
        <% region = ["アジア","北米","アフリカ","欧州"] %>
        <% region.each do |r| %>
          <%= f.label :region, r %>
          <%= f.check_box :region , {multiple: true},r,nil %>
        <% end %>
      </div>
    <b>希望する支援内容（複数選択可）</b>

    <div class="each-answer">
      <% objective = ["人材獲得","販路開拓","拠点設立","協業連携","市場調査","共同研究","資金調達","ビジネス戦略策定", "貿易実務"] %>
      <% objective.each do |o| %>
        <div class="objective-form">
        <%= f.label :objective, o %>
        <%= f.check_box :objective , {multiple: true},o,nil %>
        </div>
      <% end %>
    </div>
    </div>
    <div class="result-button-container">
      <button class="btn btn-primary result-button" type="submit">診断結果へ
      </button>
    </div>
    </div>
  <% end %>
</div>
